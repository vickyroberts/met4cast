
import logo from '../assets/logo-met.jpg';

const Header = () => {
    return (
        <header className="header">
            <img src={logo} alt="met4cast" className="logo-img"></img>
        </header>
    );
}

export default Header;